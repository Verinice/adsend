import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Ad Dashboard | AdSend",
  description: "Manage properties, pages, containers, and banners for ad injection with AdSend.",
};
